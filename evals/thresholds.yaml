# Starting points, not standards. Tune per project, and change only in dedicated commits.
# v0: latency raised 2s -> 60s because every claim needs an LLM classify call plus a web-search evaluator call.
min_pass_rate: 0.90
max_latency_p95_s: 60.0
max_mean_cost_usd: 0.10
